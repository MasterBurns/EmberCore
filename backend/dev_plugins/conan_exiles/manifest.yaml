name: "Conan Exiles Dedicated Server"
id: "conan_exiles"
category: "Survival"
engine: "steamcmd"
# AppID 443030 ist der reine Server, nicht das Spiel!
steam_app_id: 443030
executable_windows: "ConanSandboxServer.exe"
default_args:
  - "-log" # Zwingend erforderlich, damit der Server Logs für unseren neuen "Konsole"-Reiter ausspuckt!
ports:
  - 7777  # Game Port
  - 27015 # Steam Query Port
backup:
  # Sichert die komplette SQLite-Datenbank und Mod-Listen
  source_path: "ConanSandbox/Saved"
config_meta:
  # Der Standardpfad der Unreal-Engine für die Server-Einstellungen
  file_path: "ConanSandbox/Saved/Config/WindowsServer/ServerSettings.ini"
  format: "key_value"
  fields:
    - key: "ServerName"
      label: "Server-Name"
      type: "text"
      default: "EmberCore Conan Server"
    - key: "MaxPlayers"
      label: "Maximale Spieleranzahl"
      type: "number"
      default: 40
    - key: "ServerPassword"
      label: "Server-Passwort (leer = öffentlich)"
      type: "text"
      default: ""
